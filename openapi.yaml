openapi: 3.0.0
info:
  title: Hello Function API
  version: 1.0.0
paths:
  /hello:
    get:
      summary: Returns a hello message
      operationId: hello
      responses:
        '200':
          description: A successful response
          content:
            text/plain:
              schema:
                type: string
      x-google-backend:
        address: https://REGION-PROJECT_ID.cloudfunctions.net/hello-function
